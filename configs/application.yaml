app:
  env: dev

http:
  port: 8080

db:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: app
  sslmode: disable
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime: 30m
  conn_max_idle_time: 10m
  ping_timeout: 5s
  auto_migrate: true